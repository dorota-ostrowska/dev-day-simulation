from typing import Any


def log(data: Any) -> None:
    """
    Simple logging function to print data to console.
    This can be replaced with a more sophisticated logging system if needed.

    Args:
        data: Data to log, can be any type.
    """
    print(data)
